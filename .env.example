# API URL the browser will use to reach the backend (used when building the frontend).
# For local Docker: leave as is. For a server, use your host and port, e.g. http://192.168.1.100:3001/api
NEXT_PUBLIC_API_URL=http://localhost:3001/api
